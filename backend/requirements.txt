# ──────────────────────────────────────────────
# Interleet Backend — Python Dependencies
# ──────────────────────────────────────────────

# Web Framework
fastapi[standard]>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Database
motor>=3.4.0
pymongo>=4.7.0

# Validation
pydantic>=2.7.0
email-validator>=2.1.0

# Configuration
python-dotenv>=1.0.1

# Auth
authlib>=1.3.0
python-jose[cryptography]>=3.3.0
webauthn>=2.0.0

# HTTP client
httpx>=0.27.0
requests>=2.31.0

# ─── Judge Engine Dependencies ────────────────
# Docker SDK (for sandbox execution)
docker>=7.1.0

# Redis (async, for queue + cache)
redis[asyncio]>=5.0.4

# Async file I/O (workspace writes)
aiofiles>=23.2.1

# WebSocket support (FastAPI built-in uses starlette)
websockets>=12.0

# ─── AI / LLM ────────────────────────────────
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-groq>=0.1.0
langgraph>=0.1.0
groq>=0.9.0
onnxruntime>=1.16.0
mediapipe>=0.10.0

# ─── File / Media ─────────────────────────────
PyMuPDF>=1.24.0
cloudinary>=1.40.0

# ─── Utilities ────────────────────────────────
python-slugify>=8.0.4
pinecone

# ─── Face Recognition (optional deep learning) ─
# deepface + tf-keras requires Python ≤3.12 (TensorFlow limitation).
# face_recognition (dlib) requires Visual C++ on Windows.
# Current implementation uses ArcFace ONNX (already via onnxruntime above)
# via app/services/deepface_service.py which is API-compatible with deepface.
#
# To enable deepface on Python 3.12 venv:
#   pip install deepface tf-keras
# Then uncomment the DEEPFACE_AVAILABLE block in deepface_service.py.
#
# deepface>=0.0.93   # Python 3.12 only
# tf-keras            # required by deepface
